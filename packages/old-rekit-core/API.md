## Rekit core API reference

Rekit core provides the core APIs for managing Rekit elements like features, components, actions, etc.

You can create plugins using Rekit core APIs to extend Rekit's capability.
