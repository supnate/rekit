import React, { Component } from 'react';

// This is the page used to show when all tabs closed.
export default class BlankPage extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="home-blank-page">
        Nothing to show.
      </div>
    );
  }
}
