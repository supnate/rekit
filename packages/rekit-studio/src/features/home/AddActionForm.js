import React, { PureComponent } from 'react';
import PropTypes from 'prop-types';

export default class AddActionForm extends PureComponent {
  static propTypes = {

  };

  render() {
    return (
      <div className="home-add-action-form">
        Component content: home/AddActionForm
      </div>
    );
  }
}
