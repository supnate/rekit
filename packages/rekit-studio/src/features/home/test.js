const c = (
  <h1 title="abc">
    {stor.isk && <div>a</div>}
    Something {2222} wrong.
    <a href="">abc</a>
  </h1>
);

const a = (
  <div className="home-element-page">
    {data.isPic && (
      <div className="pic-wrapper">
        <img src={`/${codeFile}`} alt={codeFile} />
      </div>
    )}
  </div>
);
