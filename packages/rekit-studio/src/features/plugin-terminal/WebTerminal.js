import React, { Component } from 'react';

export default class WebTerminal extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="plugin-terminal-web-terminal">
        Component content: plugin-terminal/WebTerminal
      </div>
    );
  }
}
