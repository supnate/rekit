'use strict';

/**
 * This function will be executed while the plugin is installed via 'rekit install <plugin-name>'.
 * The function is expected to be able to run multiple times. That is should handle the case if user
 * accidently run it multiple times. It should do nothing and give proper feedback if has been installed.
**/
function install() {
}

module.exports = install;
