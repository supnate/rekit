import React, { PureComponent, PropTypes } from 'react';

export default class Abc extends PureComponent {
  static propTypes = {

  };

  render() {
    return (
      <div className="component-abc">
        Component content: components/Abc
      </div>
    );
  }
}
