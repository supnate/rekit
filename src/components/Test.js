import React, { PureComponent, PropTypes } from 'react';

export default class Test extends PureComponent {
  static propTypes = {

  };

  render() {
    return (
      <div className="component-test">
        Component content: components/Test
      </div>
    );
  }
}
